/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "picsum.photos",
      "www.supplychainmagazine.nl",
      "static.tildacdn.info",
    ],
  },
};

export default nextConfig;
